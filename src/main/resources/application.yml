server:
  port: 8081
  error:
    include-message: always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: shaphat
    url: jdbc:mysql://localhost:3306/rancard
    password: 12hashedpageS1.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true
